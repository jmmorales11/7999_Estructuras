#include <stdio.h>
#include <stdlib.h>
#include "validar.h"
main(){
	char cedula[10];
	int aux;
       str_cpy(cedula,ingresarDatosEnteros("\nIngrese la cedula: "));
       for (int i =0;i<10;i++){
       	aux=funcion_atoi(cedula[i],strlen(cedula));
       	if
       	
	   }
}

